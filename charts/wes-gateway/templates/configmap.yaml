apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wes-gateway.fullname" . }}-backends
  labels:
    {{- include "wes-gateway.labels" . | nindent 4 }}
data:
  backends.yaml: |
    {{- include "wes-gateway.registry" . | nindent 4 }}
